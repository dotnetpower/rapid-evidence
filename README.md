# rapid-evidence

A policy-governed burst evidence collection toolkit with CLI, API, and warm-pool controls.

## Development

- Create a 3.12 virtual environment with `uv venv .venv --python 3.12.10`
- Install the package with `uv pip install --python .venv/bin/python -e .`
- Run tests with `.venv/bin/pytest -q`
