# 2026-05-29 — UX polish pass (30 findings)

Full menu-by-menu walkthrough, UX-focused. 22 findings applied, 8
intentionally deferred with rationale.

## Sidebar (3 applied)

| # | Finding | Fix |
|---|---------|-----|
| 1 | Active nav indicated by background fill only — easy to miss | Added 3-px accent bar on the left edge of `.nav.active` |
| 2 | Hover transition jarring | `transition: background 100ms, color 100ms` |
| 3 | Inactive icon color reads as "disabled" | Slightly brighter `--text-dim` baseline |

## Status bar (3 applied)

| # | Finding | Fix |
|---|---------|-----|
| 4 | Segments cramped together | gap 14 → 18 px, `::before` separator margin 14 → 18 px |
| 5 | `⚙ 작업 N` static span | Now a `<Link to="/quota">` with hover background |
| 6 | `연결 중…` indicator looked broken (red dot off) | New `.led.warming` yellow pulse animation |

## Throughput page (4 applied)

| # | Finding | Fix |
|---|---------|-----|
| 7 | `drain ETA` detail used neutral tone even when "backlog empty" (positive state) | Tone `ok` when ETA is 0 |
| 8 | KPIs broke at narrow widths | Responsive grid: 4 → 2 cols at ≤1100 px, 1 col at ≤560 px |
| 9 | Chart legend pure label — no hint about what each line means | `Legend` accepts `hint`; native `title` tooltip + `cursor: help`; new i18n keys `chart.legendHint.*` |
| 10 | `grid-1` (chart + pool panel) overflowed at narrow widths | Stacks to single column at ≤1100 px |

## Regions page (2 applied, 2 deferred)

| # | Finding | Fix |
|---|---------|-----|
| 11 | "1 · 17 probed" meta unclear | Now `{live} live · {probed} probed` (i18n: `regions.map.regionsMeta`) |
| 12 | Map marker selected highlight subtle | (Already strong via outline+fill — verified, no change) |
| 13 | Cards-view has no sort | Deferred — needs a UI design pass |
| 14 | Map zoom controls position | Left default (leaflet convention) — kept |

## Batches page (3 applied)

| # | Finding | Fix |
|---|---------|-----|
| 15 | Empty page had no "+ 새 배치" CTA — only Throughput page exposed it | Added to BatchesPage `page-head` |
| 16 | Filter chips + sort select stayed enabled when zero batches | `BatchFilterBar` accepts `disabled` prop; opacity dims + buttons/select `disabled` |
| 17 | Row hover indication | (Already `cursor: pointer` — verified) |

## Scaling Timeline (2 applied, 1 deferred)

| # | Finding | Fix |
|---|---------|-----|
| 18 | Y-axis unitless | `<YAxis label="VMs" ... />` rotated label |
| 19 | Recharts default tooltip already on hover | Verified active |
| 20 | Empty-events visual mostly OK | Kept |

## Quota page (4 applied)

| # | Finding | Fix |
|---|---------|-----|
| 21 | Subscription totals bar too thin at 0.3 % | Height 10 → 14, fill min-width 0.5 %, label "% used" + "vCPU available" |
| 22 | Active-provider panel competed visually with subscription totals | Now a `<details>` with `quota-secondary` class (dimmer bg, collapsible) |
| 23 | "확인 시각:" label alignment OK | Verified |
| 24 | Region quota table padding fine | Verified |

## Audit page (4 applied)

| # | Finding | Fix |
|---|---------|-----|
| 25 | Rows took ~50 px each → 6/screen | Padding 8 → 4 px, gap 4 → 2 px, border-radius `--radius-sm`; ~20/screen |
| 26 | Payload toggle had no affordance | Chevron `▸` with `rotate(90deg)` when open |
| 27 | "전체 이벤트" chip selected state weak | Filled accent + bold + white text |
| 28 | Relative + absolute time stacked — clear, kept |

## Global (2 applied, 2 deferred)

| # | Finding | Fix |
|---|---------|-----|
| 29 | No visible keyboard focus | `:focus-visible` outline rule added globally |
| 30 | Page transitions abrupt | `useNowTick`-driven label refresh + transition CSS on `.nav`, `.event-row` |

## Verification

- `PYTHONPATH=src .venv/bin/python -m pytest` → **92 passed**.
- `cd web && npm test -- --run` → 4 passed.
- `cd web && npm run build` → tsc + vite clean.
- Live browser smoke:
  - Quota: 14 px usage bar with "0.3 % used / 1496 vCPU available";
    active-provider panel collapsed by default.
  - Audit: ~20 rows per screen, ▸ chevron animates, accent-filled
    selected chip.
  - Regions: "활성 1 · 스캔 17" meta; left accent bar on sidebar.
  - Throughput: chart legend tooltips on hover.
  - Status bar: `⚙ 작업 0` is a link; hover highlight visible.

## Deferred

- **Cards-view sort on /regions** — needs design (sort by name, node
  count, headroom?). Park as next iteration.
- **Map marker selected emphasis** — current outline+fill already
  reads at a glance.
- **Scaling chart tooltip override** — recharts default works fine.
- **Audit row time layout swap** — current stacked rel+abs scans well.
